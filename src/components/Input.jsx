import React from "react";

export const TextInput = () => {
  return (
    <input type="text" />
  );
};

export const DateInput = () => {
  return (
    <input type="date" />
  );
};

export const TelInput = () => {
  return (
    <input type="tel" />
  );
};
